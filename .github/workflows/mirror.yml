name: Sync from baocommunity

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses:织 mirror-sync@master
        with:
          github-token: ${{ secrets.MIRROR_TOKEN }}
          source-repo: 'baocommunity/2140-social'
          destination-repo: '2140wtf/2140-social'
