/**
 * STUB (voice cut). Armada's `CallStageSlot` is the top-of-chat host into
 * which the active LiveKit call's stage portals. The ₿AO build ships no voice
 * stack (no CallProvider / LiveKit), so the slot renders nothing under the
 * same props.
 */
export function CallStageSlot({ active: _active }: { active: boolean }) {
  return null;
}
